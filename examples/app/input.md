# Demo

This is just some **demo** and `something that should not get highlighted`.

Just go and `run <script>`.

```javascript
var a = 4;

function foo() {
    console.log('foo');
}

console.log(a + 5);
```

